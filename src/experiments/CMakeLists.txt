cmake_minimum_required(VERSION 2.8)

INCLUDE_DIRECTORIES(${NICE2_SOURCE_DIR}/base)
INCLUDE_DIRECTORIES(${NICE2_SOURCE_DIR}/inference)
INCLUDE_DIRECTORIES(${NICE2_SOURCE_DIR}/syntree)

SET(LM_COMPARE lm_compare.cpp)
ADD_EXECUTABLE(lm_compare ${LM_COMPARE})
TARGET_LINK_LIBRARIES(lm_compare syntree)
